[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[project]
name = "pfm-segmentation"
version = "0.1.0"
requires-python = ">=3.11"

[tool.hatch.build.targets.wheel]
packages = ["models"]
